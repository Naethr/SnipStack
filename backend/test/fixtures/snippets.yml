rails_routes:
  title: Rails routes
  description: Show all Rails routes.
  language: bash
  code: bin/rails routes
  tags: rails, routes, debug

react_fetch:
  title: React fetch example
  description: Basic fetch call to an API endpoint.
  language: javascript
  code: fetch('/api/v1/snippets')
  tags: react, fetch, api